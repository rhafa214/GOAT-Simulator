# Avatar Models Directory

This directory contains the 3D models and the manifest for the Avatar 2.0 system.

The `manifest.json` file controls which models are available and defines their metadata (license, rig type, animations, etc.).
No unlicensed assets should be added to this directory.
